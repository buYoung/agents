interface:
  display_name: "Codex Orchestrator"
  short_description: "Coordinates eight leaf agents from the main session"
  default_prompt: "$codex-orchestrator Coordinate this request with the eight allowed leaf agents."

policy:
  allow_implicit_invocation: false
