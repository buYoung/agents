interface:
  display_name: "Codex 오케스트레이터"
  short_description: "main session이 8개 leaf agent를 직접 조정합니다"
  default_prompt: "$codex-orchestrator 이 요청을 8개 허용 leaf agent로 조정해 주세요."

policy:
  allow_implicit_invocation: false
